# 研发日志（dev_log）

记录原则：
- 只记录重要事件、关键决策、重大问题与解决方案
- 以“可复现”为目标写清：现象 → 原因 → 处理 → 结果

---

## 2025-12-28

### 项目恢复与可运行性
- 事件：项目出现后端入口文件缺失、前端 package.json 缺失等导致无法启动的问题。
- 处理：补齐后端 FastAPI 入口与服务模块结构；补齐前端 Vite/React/Electron 所需的 package.json 与基础页面结构。
- 结果：前后端可启动，核心页面可加载。

### 前端问题定位与修复
- 现象：浏览器报错 net::ERR_ABORTED，资源加载失败。
- 原因：缺失样式文件（例如 App.css / app.css 类文件引用不一致）。
- 处理：补齐/修正样式文件使资源可正常加载。
- 结果：前端资源加载恢复正常。

### 端口占用与环境问题
- 现象：开发端口（如 5173）被占用导致启动失败。
- 处理：结束冲突进程后重新启动开发服务。
- 结果：开发服务可正常运行。

### 文档与依赖说明
- 事件：为便于协作与复现，补充项目使用说明与依赖清单。
- 产出：README.md、backend/requirements.txt。

### 待办（当日遗留）
- 清理仓库中的敏感信息（例如包含 API Key 的本地文件）。
- 补齐 .gitignore，避免将构建产物与本地配置提交到仓库。
- 初始化 Git 并推送到远程仓库：https://github.com/Chiward/xuanchaungao2.git

### 推送前仓库清理
- 现象：项目根目录存在包含真实 API Key 的本地文件，存在泄露风险。
- 处理：删除敏感文件；新增 .gitignore 忽略 .env、本地构建产物与敏感文件；后端改为仅从环境变量或 .env 读取 DEEPSEEK_API_KEY；同步更新 README 的配置方式。
- 结果：仓库不再包含明文密钥，推送准备就绪。

### 推送到 GitHub
- 现象：首次推送失败，Git 尝试连接本机代理端口导致无法访问 GitHub。
- 原因：仓库级 git 配置中存在 http.proxy/https.proxy 指向 127.0.0.1。
- 处理：移除仓库级代理配置后重新推送。
- 结果：main 分支已成功推送到远程仓库。
